# Next Features

This is the running parking lot for parked and future features.
Add new ideas here when the user says "later" or asks for something
out of the current scope, instead of starting the work immediately.
Keep each entry short: title, one-line description, status.

## Next feature list

1. **Persistent failure log + valid-vs-nonsense review** — maintain a
   persistent log of stage failures across runs; later, review the log to
   verify which failures were valid and which were nonsense, and
   tune/remove the bad checks. (Per-run attempt history already exists
   in-session; this is the cross-run persistent log + review pass.)
   Status: planned, not started.

2. **Per-tool model selection with speed info** — let the user select which
   model each tool/agent uses, and show speed information for the models.
   (User phrasing: "select model inside tools and their speed".)
   Status: planned, not started.
